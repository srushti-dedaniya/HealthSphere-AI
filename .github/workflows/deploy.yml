name: Deploy

on:
  workflow_run:
    workflows: [Frontend CI, Backend CI]
    branches: [main]
    types: [completed]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Placeholder
        run: echo "Deploy job pending — choose a hosting target to configure."
